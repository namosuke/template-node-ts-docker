{
  "name": "template-node-ts-docker",
  "version": "1.0.0",
  "author": "https://github.com/namosuke",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.2"
  },
  "devDependencies": {
    "@types/node": "^18.7.16",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.3"
  },
  "scripts": {
    "dev": "ts-node -r dotenv/config src/app.ts",
    "build": "tsc",
    "start": "node -r dotenv/config out/app.js || echo NOTE: run `yarn build` first"
  }
}
